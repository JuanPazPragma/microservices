server:
  port: 8099

eureka:
  instance:
    hostname: localhost

  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8099/eureka/

spring:
  security:
    user:
      name: root
      password: s3cr3t